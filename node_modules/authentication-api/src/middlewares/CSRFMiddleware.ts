import csurf from "csurf";

export default csurf({
    cookie: {
        key: 'XSRF-TOKEN',  // Nome do cookie onde o token será armazenado
        httpOnly: false     // O cookie pode ser acessado pelo frontend
    }
});